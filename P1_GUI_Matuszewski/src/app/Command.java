package app;

/**
 * Enum zawierajacy liste polecen obslugiwanych w aplikacji
 * @author Jedrzej Matuszewski
 * @version 1.0
 */
public enum Command {
	SUM, AVG, MIN, MAX, ADD_VALUE, CLEAR, FILL_RANDOM, SAVE_TO_FILE, IMPORT_FROM_FILE, CLOSE_APP, PRINT_DATE
}
